Here are a few helpful python packages that you'll need to install before using `automate_example.ipynb`:

For MacOS/Linux:
```
python3 -m pip install pyautogui
conda install time
conda install imageio
conda install moviepy
```
